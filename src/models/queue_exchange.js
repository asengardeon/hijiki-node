class HijikiQueueExchange {
    constructor (name, exchange_name, options = {}) {
        this.name = name
        this.exchange_name = exchange_name
        this.options = options
    }    _
}

export default HijikiQueueExchange
